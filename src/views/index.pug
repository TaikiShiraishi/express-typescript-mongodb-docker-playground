extends layout

block content
  div
    a(href="/update") 投稿画面へ
  h1 投稿一覧
  section
    each msg in messages
      div
        h2 #{msg.username}
        time #{msg.date}
        p #{msg.message}
      if msg.image_path
        img(src=msg.image_path)

